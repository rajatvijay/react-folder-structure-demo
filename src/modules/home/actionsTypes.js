// Prefix should be used to namespace actions between diff modules
// Prefix should be same as that of the name of the module, but in capitalize case
const PREFIX = `HOME_`;

// Separating actions and action types, gives you a clear idea of all the actions in you module
export const TOGGLE_SIDEBAR = `${PREFIX}TOGGLE_SIDEBAR`;
